# Models — pre-deploy checks

Things Malka must know to finish this card. Skip what Talk already settled.

- Parameters, what each does, and the scale each is on.
- Task structure the generating function faces.
- New definition or structural variant (a variant is a new folder).
- If a recovery study will use the pair: the three generating/fitting agreements.

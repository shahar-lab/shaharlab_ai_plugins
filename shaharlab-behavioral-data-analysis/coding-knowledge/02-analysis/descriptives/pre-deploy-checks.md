# Descriptives — pre-deploy checks

Things Malka must know to finish this card. Skip what Talk already settled.

- Which variables get described.
- Grouping the tables break down by.
- Which measures get a distribution figure rather than a mean and SD.
- Each named measure exists as a scored column in `data/processed/`.

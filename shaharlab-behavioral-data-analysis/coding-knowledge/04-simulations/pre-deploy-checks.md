# Simulation — pre-deploy checks

Things Malka must know to finish this card. Skip what Talk already settled.

Folder name and new/clone/repair live on the Plan Card (`interview-plan-card.md`).

- Recovery only: environment (trials/blocks, task constants); population (n agents, location and scale per parameter); which `models/` definition generates and which is fitted; sampler; success criteria (correlation, bias, precision — propose conventional defaults).
- Skip the recovery bullets when this job is only a `models/` pair.
